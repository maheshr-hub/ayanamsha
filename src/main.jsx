import React from 'react';
import ReactDOM from 'react-dom/client';
import VedicSkyMap from './VedicSkyMap.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VedicSkyMap />
  </React.StrictMode>
);
